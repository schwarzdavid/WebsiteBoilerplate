{
	"name": "<%= name %>",
	"authors": [
		"<%- author %>"
	],
	"description": "",
	"main": "src/index.html",
	"license": "MIT",
	"private": true,
	"overrides": {
		"bootstrap": {
			"dependencies": {
				"jquery": "^3.2.1"
			}
		}
	},
	"dependencies": {
		<% if(dependencies.indexOf('bootstrap') >= 0){ %>
		"bootstrap": "3.3.7",
		<% } %>

		<% if(dependencies.indexOf('jquery') >= 0){ %>
		"jquery": "3.2.1"
		<% } %>
	}
}
